// Notation
// ; --> Delimited the final of a line
const b = 4;

b + 4;

// . --> It is utilized on the objects for access to the attributes
// mobile.width


// [] --> Lists, arrangements or arrays
const ar = [1, 2, 3, 4]
console.log(ar[2])

// () --> Functions
function sum(a, b) {
    // It is written the function
}


// {} --> Keys for objects, functions and structures of control
const mobile = {
    width: 5,
    hight: 10
}
if (true) {
    // Everything between the keys
    const constant2 = "Hi"
}

for ()